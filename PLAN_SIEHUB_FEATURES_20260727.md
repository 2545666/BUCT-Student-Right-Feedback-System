# SIEHUB 功能扩展实施计划（2026-07-27）

## 目标与边界

本轮在现有 SIEHUB、SIEVOX、SIEBridge 和部门介绍编辑器基础上完成以下能力：

1. 部门介绍页的顶部封面和文本介绍支持图片背景。
2. 成员管理选项卡降低饱和度、加深颜色，并随主题正确适配。
3. SIEBridge 管理员可删除已审核资料，操作受权限、确认和审计约束。
4. 届次与成员归档只允许终极管理员操作，全部门统一生效。
5. SIEBridge 作为与 SIEVOX 同级的独立业务页面运行。
6. SIEHUB 左上角 Logo 放大并移除白底感。
7. 各部门可发布通知，首页消息中心按部门和时间聚合展示。
8. 首页主视觉改为“国教空间”微信公众号入口，并在具备公众号接口权限时同步文章。

不在本轮开放任意 HTML、CSS 或脚本编辑；部门内容继续使用受控数据结构。公众号自动同步不在前端保存 AppSecret，也不以抓取网页的方式绕过微信接口权限。

## Phase 0：现有实现与可用接口

### 现有实现

- `backend/server.js` 已包含部门介绍页、组织治理和站内通知模型/路由。
- `backend/organization.js` 已通过 capability 描述模块权限，`manage_cohort_archive` 当前仅出现在终极管理员治理窗口。
- `backend/siebridge.js` 已包含课程、资料、审核、PDF 预览和下载接口，但没有删除已审核资料接口。
- `frontend/src/DepartmentIntroduction.jsx` 已支持 hero、text、image、video、duties、contact 六类受控区块。
- `frontend/src/App.jsx` 当前在学术科技部页面内部嵌入 SIEBridge，尚未形成与 SIEVOX 同级的 `appSurface`。
- 现有 `/api/notifications` 是面向单个账号的未读通知，不等同于面向全体学生的部门公告流。

### 允许使用的项目接口与模式

- 复用 `authenticate`、`logAction`、`getHubModuleAccess` 和 capability 权限模式。
- 复用部门介绍媒体上传目录和 `/api/department-intro-assets` 静态资源映射。
- 复用 Node.js 18 原生 `fetch` 调用微信官方接口，不新增不必要的 HTTP 依赖。
- 复用 React 现有 `appSurface` 页面切换模式和 SIEVOX 返回 SIEHUB 的导航布局。

### 外部接口约束

- 微信公众号自动同步需要公众号后台提供 AppID、AppSecret，并为服务器 IP 配置白名单。
- 仅在公众号类型和认证状态具备草稿/发布能力时，启用已发布文章同步。
- 未提供凭据或接口无权限时，首页仍展示“国教空间”封面、二维码/主页链接，并允许管理员手动录入文章链接。

## Phase 1：权限与数据模型

### 1.1 SIEBridge 删除已审核资料

- 新增 capability：`delete_siebridge_approved_resource`。
- 仅授予现有 SIEBridge 审核主体：分管学术科技部的主席团成员、团委学生兼职副书记和终极管理员。
- 新增 `DELETE /api/siebridge/resources/:id`。
- 只允许删除 `approved` 资料；待审核和已驳回资料继续走审核流程，不复用该接口。
- 请求体要求 `confirmation` 与资料标题或固定确认短语匹配。
- 删除数据库记录前校验实际文件路径必须位于 `backend/siebridge_uploads` 内。
- 操作写入 `AuditLog`；文件删除失败时返回错误，不产生“记录已删但文件残留”的假成功。
- 课程本身不随最后一份资料删除；无资料课程显示为空状态。

### 1.2 归档权限统一

- `manage_cohort_archive` 只保留在终极管理员窗口。
- 所有归档和届次删除接口继续使用 `ultimateOnly`，禁止通过部门 capability 间接获得权限。
- 部门管理端“成员与归档”改为只读说明，不提供归档按钮。
- 增加组织权限测试，覆盖普通学生、志愿者、部门负责人、分管主席团/副书记、终极管理员五类身份。

### 1.3 部门公告与微信文章

- 新增 `DepartmentNotice`：组织、部门、中英文标题/摘要、正文、封面、原文链接、来源、发布时间、状态、创建人、发布时间戳。
- 新增唯一外部来源键，防止微信公众号文章重复同步。
- 部门负责人、团委学生兼职团干部、对应分管主席团/副书记和终极管理员可维护本部门公告。
- 学生读取接口只返回已发布公告；管理接口按权限返回草稿和已发布公告。

## Phase 2：部门介绍背景图片

- hero 和 text 区块增加 `backgroundImageUrl`、`overlayOpacity`、`textTone`。
- 编辑器复用现有图片上传接口，提供“上传背景图”“移除背景图”、遮罩强度和明暗文字模式。
- 后端白名单清洗字段；只接受平台上传资源 URL 或明确允许的 HTTPS URL。
- 预览和学生端使用相同渲染组件，避免编辑态与发布态不一致。
- 移动端保证背景裁切、标题换行和正文可读性。

## Phase 3：页面与视觉调整

### 3.1 SIEBridge 独立页面

- 新增 `appSurface = 'siebridge'`，从学术科技部学生服务卡进入独立页面。
- 独立页面顶部提供 SIEBridge 品牌、用户身份、主题/语言切换，以及名字左侧的“返回 SIEHUB”。
- 学生视图包含课程检索、课程详情、上传资料、添加课程和我的审核状态。
- 有审核权限的账号可在同一独立页面切换“学生端 / 审核管理”。
- 学术科技部入口页仍保留“部门介绍 / 学生服务入口 / 通知与活动”三个入口。

### 3.2 成员管理选项卡

- 使用低饱和度主题色作为底色、深色文字和清晰边框。
- 浅色/深色主题分别定义 active、hover、focus、disabled 状态。
- 固定控件高度和最小宽度，英文标签换行但不挤压相邻模块。

### 3.3 左上角 Logo

- 统一使用透明背景的 `SIEHUB_LOGO.png`，移除包裹元素的白色底板和多余阴影。
- 桌面端和移动端分别设置稳定尺寸，保持比例、不裁切、不推动导航栏高度。

## Phase 4：首页消息中心

- 首页新增消息中心，默认按发布时间倒序展示全部门已发布通知。
- 提供部门筛选、时间范围筛选和“仅看最新”状态。
- 每条通知展示部门、发布时间、标题、摘要、来源和原文链接。
- 部门学生端“通知与活动”卡进入该部门过滤后的消息列表。
- 管理端增加公告编辑入口，支持草稿、发布、撤回和封面图片。
- 本模块与现有账号级 `/api/notifications` 分开，避免把公告误标为个人未读提醒。

## Phase 5：“国教空间”微信公众号接入

### 5.1 可立即上线的入口

- 将首页现有 `SIE / PLATFORM 01` 主视觉替换为“国教空间”公众号封面。
- 封面支持后台配置图片、二维码、公众号主页/文章链接和备用说明。
- 点击封面在新窗口打开已配置链接；未配置时仅展示二维码和公众号名称。

### 5.2 自动同步适配器

- 新增后端微信适配器，缓存 `access_token` 并在过期前刷新。
- 通过公众号已发布文章能力增量拉取文章，转换为 `DepartmentNotice`，来源标记为 `wechat_mp`。
- 以外部文章 ID 和更新时间去重，定时任务失败不影响主服务启动。
- 提供终极管理员手动触发同步和查看最近同步状态的接口。
- 环境变量：`WECHAT_MP_ENABLED`、`WECHAT_MP_APP_ID`、`WECHAT_MP_APP_SECRET`、`WECHAT_MP_ACCOUNT_NAME`、`WECHAT_MP_ACCOUNT_URL`、`WECHAT_MP_SYNC_INTERVAL_MINUTES`。
- 日志不得输出 AppSecret 或完整 access_token。

## Phase 6：验证与部署

- 后端测试：权限矩阵、已审核资料删除、路径越界保护、部门公告可见性、微信同步去重和失败降级。
- 前端构建：`npm run build`。
- 后端测试：`npm test`。
- 响应式验收：桌面 1440px、笔记本 1280px、移动端 390px；中文和英文各验证一次。
- 交互验收：背景图上传/移除、SIEBridge 独立返回、删除二次确认、公告筛选、公众号无凭据降级。
- 更新 `README.md`、`.env.example` 和远端部署交接；部署时保留 MongoDB 和三个上传目录。

## 需要部署方提供的公众号信息

自动同步上线前需要提供：

1. “国教空间”公众号 AppID。
2. AppSecret（仅写入远端后端环境变量，不提交仓库）。
3. 公众号类型、认证状态和已开通接口清单。
4. 服务器出口 IP，以便加入公众号后台白名单。
5. 公众号封面图、二维码和期望跳转链接。

在这些信息未齐全前，其余功能和公众号封面入口可以先行上线，自动同步保持关闭。
