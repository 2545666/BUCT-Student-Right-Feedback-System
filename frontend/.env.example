VITE_API_BASE=/api

# Optional independent platform URLs.
# When these are set, SIEHUB entries navigate to these hosts; the same frontend build
# can also auto-open the matching platform when served from that host.
VITE_SIEVOX_URL=https://sievox.cn
VITE_SIEBRIDGE_URL=https://siebridge.sievox.cn
